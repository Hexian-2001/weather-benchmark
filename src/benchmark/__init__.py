"""WeatherBench-MingYang-Tech — weather ML model benchmark platform.

A standardized, reusable, auditable evaluation system for internal model iteration:
register → compliance check → reforecast → score → archive → leaderboard.
"""

__version__ = "0.1.0"
